# P9 — Accessibility and responsive re-verification          agent: C′   started/finished: 2026-09-14 17:50 / 18:20   mirror: :8143
findings:   Blocker 0 · High 0 · Medium 0 · Low 0
| ID | sev | class | where | one line |
|---|---|---|---|---|
| (none) | — | — | — | all seven method steps re-verified clean; see records below for the checked/no-finding evidence |
ledger:     done E016,E023,E024,E037,E039,E089,E183   blocked none
suites cited: plan8-keyboard 8/8, plan8-motion 8/8, plan8-mobile 16/16, plan8-chrome 16/16, plan8-contrast 34/35, plan2-contrast 42/42, plan5c-eyebrow 5/5, plan5c-brandink 6/6, brandtext 36/47 (11 failing, ratchet ≤13), plan4-public 27/27, plan4-admin 19/19, plan10-rfqscroll 24/24, plan10-adminrows 15/15, plan10-adminnav 25/25, plan10-helpwidth 21/21
instruments: _harness/audit9-p9-keyboard.js — real Tab/Enter/Escape (never `.focus()`) through the mobile drawer, both mega-menus, the FAQ accordion, the dashboard's sortable headers, and the contact form's tab switch, checking `:focus-visible`, focus-enters-overlay, and Escape-closes-from-inside · _harness/audit9-p9-reflow.js — WCAG 1.4.10 zoom simulated as a shrunk layout viewport (720px = 1440/2, 320px = 1280/4), `<html>` scrollWidth vs clientWidth plus a per-element scan excluding `.sr-only` and legitimate horizontal scrollers · _harness/audit9-p9-largetext.js — root `font-size:24px`, scanning for a fixed-px `height` smaller than `scrollHeight`, excluding `.sr-only`, `aria-hidden` subtrees (the contact-form honeypot) and legitimate vertical scrollers (`.ipc-scroll-cue`)
artifacts:  _harness/out/audit9/P9/keyboard.json · _harness/out/audit9/P9/reflow.json · _harness/out/audit9/P9/largetext.json · _harness/out/audit9/P9/semantics.json
out of brief: none (the `aria-current` gap noted while running P6 was recorded there, not duplicated here — GUARDRAILS §3.7, "not audited by the agent that noticed it")
[UNVERIFIED]/[UNSOURCED]: axe (step 6) NOT RUN — optional per brief, and every hit it could produce still needs reproduction through steps 2-5 before it counts (GUARDRAILS "six probe defects" precedent); steps 2-5 already cover keyboard/reflow/large-text/semantics directly, and 0 findings surfaced there, so the marginal value of a temporary `npx @axe-core/cli` pass against the same DOM was judged not worth the added reproduction cost inside this pass's time budget. Recorded as NOT-MEASURED rather than silently skipped.
self-corrections: audit9-p9-keyboard.js's dashboard-sort check first looked for `aria-sort` on `document.activeElement` itself and found nothing in 40 tabs (0/1 "failing") — the attribute correctly lives on the ancestor `<th>` per the WAI-ARIA sortable-table pattern (App.jsx:10618), not the nested `<button>` that receives focus (App.jsx:10642); the probe was wrong, not the page. Fixed to read `el.closest('th')` and to detect the button via `data-sort-key` (present regardless of sort state) rather than presence of `aria-sort` (only set on the currently-sorted column). audit9-p9-reflow.js and audit9-p9-largetext.js both initially flagged `.sr-only` elements, and largetext additionally flagged the contact form's `aria-hidden` honeypot (App.jsx:5290-5294) and the `/products` sidebar's intentionally-scrollable `.ipc-scroll-cue` panel (App.jsx:8057, styled scrollbar at App.jsx:6231-6237) — all three are deliberate, correctly-hidden-or-scrollable constructions, not clipped content; excluded after inspection, per GUARDRAILS §7.1 ("the probe is not the page").
---

## Method record (steps 1-7)

**Step 1 — sweep lines.** All cited above in `suites cited`; none re-measured. `brandtext` reads 36/47 (11 failing) on this checkout, not the brief's own-quoted "34/45" — consistent with the brief's own caveat that the figure has drifted before and that the *failing count* (≤13), not the ratio, is the ratchet. 11 ≤ 13: holds.

**Step 2 — keyboard, real keys only.** `_harness/audit9-p9-keyboard.js` against `:8143`, `document.activeElement.matches(':focus-visible')` (never `.focus()` — GUARDRAILS §7.1/§7.3):

| surface | reached by Tab | `:focus-visible` ring | opens/activates | focus enters overlay | Escape closes from inside |
|---|---|---|---|---|---|
| mobile drawer (390) | yes (3 tabs) | yes | yes | yes | yes |
| Products mega-menu (1440) | yes (4 tabs) | yes | yes | yes | yes |
| Company mega-menu (1440) | yes (1 tab from a reset focus) | yes | yes | yes | yes |
| FAQ first question (1440) | yes (15 tabs) | yes | `aria-expanded` false→true on Enter | n/a (accordion, not overlay) | n/a |
| dashboard sort header (1440) | yes (33 tabs) | yes | ancestor `<th aria-sort>` null→"ascending" on Enter | n/a | n/a |
| contact "Send a Message" tab (1440) | yes (8 tabs) | yes | tab switches, first message field reachable next Tab | n/a | n/a |

0 unreachable, 0 ringless, 0 traps, 0 overlays that fail to close from inside. Admin keyboard reachability/focus/naming at 390 and 1440 is cited, not re-driven here — `plan10-adminnav` (25/25, includes Sign Out CSRF check), `plan10-helpwidth` (21/21), `plan4-admin` (19/19, focus+naming), `plan8-chrome` (16/16) already exercise it across 4 viewports × 3 signed-in pages.

**Step 3 — reflow.** `_harness/audit9-p9-reflow.js`, 11 routes (10 public + one product detail) × 2 zoom arms (720px layout viewport = 1440 physical /2, 320px = 1280 physical /4). `<html>` `scrollWidth === clientWidth` on all 22 page/arm combinations — 0 page-level horizontal scroll. Per-element scan (excluding `.sr-only` and declared horizontal scrollers) found 4 non-exempt hits, all inspected and found not to be reflow failures:
- Home's `#ipc-sec-heroTrust` certifications ticker (scrollWidth 4676 vs clientWidth 720/320) — the duplicated-content auto-scrolling ticker already documented in the crawl's `ariaSnapshot` ("focus to pause the scrolling"); clipped by a deliberate `overflow:hidden` ancestor, all content present in the DOM, pausable on focus. Not a reflow failure.
- `/dashboard`'s table wrapper (`isScroller:true`, 1230/670) is the brief's own named exception — a spec-table-style scroller — plus 4 `<td>` cells 3px over their column width inside that same scroller (contained, not clipped).
- `/dashboard` mobile-card `min-w-0` flex children, 2-10px over at 400% only — the standard Tailwind ellipsis-truncation pattern (full text stays reachable on the product's own page).
0 genuine WCAG 1.4.10 failures.

**Step 4 — large text.** `_harness/audit9-p9-largetext.js`, same 11 routes, root `font-size: 24px !important`. 0 elements clipped by a fixed px height after excluding `.sr-only`, the contact form's `aria-hidden` honeypot (App.jsx:5290-5294 — correctly hidden from every real user, sighted or AT, so its own box metrics are not a UI defect), and the `/products` sidebar's intentionally vertically-scrollable panel. 0 findings.

**Step 5 — semantics (from the crawl's accessibility snapshots).** Queried, not read whole, per GUARDRAILS §token-discipline:
- Every label-dependent control named: 594 `<input>`/`<select>`/`<textarea>` (non-hidden) across all 293 crawl records, 0 with no `labelText` (`<label for>`/`aria-label`/`aria-labelledby`) — consistent with `plan4-public` 27/27 and `plan4-admin` 19/19.
- Buttons not named by a bare glyph: regex-scanned every `button "TEXT"` token in all 67 pages' `ariaSnapshot`s for a name with no letters/digits (e.g. a lone "▼" or "✕") — 0 hits; every decorative glyph (▼ ✕ → ⇅) is `aria-hidden` beside real text, per App.jsx:10650-10658 and the mega-menu triggers' full accessible names ("Expand Polyolefin Heat Shrink product list: ▼" etc., confirmed in the raw `ariaSnapshot`).
- `role="alert"` for form errors: contact form error banner (App.jsx:4755-4756, `role="alert" aria-live="polite"`) and the product-detail "part not found" banner (App.jsx:9651-9653, T2.8, `role="alert"`) both present.
- Live region for async catalog states: `CatalogSkeleton` (App.jsx:12723-12726) carries `aria-busy="true" aria-live="polite"` plus a `sr-only` "Loading the product catalog…" announcement; the contact success panel uses `role="status" aria-live="polite"` (App.jsx:4963-4967).
0 findings. (One structural observation — the current admin/nav tab is marked only by a CSS class, with no `aria-current="page"`, unlike the public Breadcrumb which does carry it — was noticed while running P6 and recorded there under "Out of brief" rather than duplicated as a P9 finding here, per GUARDRAILS §3.7's "not audited by the agent that noticed it.")

**Step 6 — axe.** Not run. See `[UNVERIFIED]/[UNSOURCED]` above for why.

**Step 7 — colour.** `WHATS_LEFT.md` §2/§3 brand-contrast decisions and GUARDRAILS §7.3 are closed and not re-litigated. `brandtext` (the only suite scoring "under the glyphs on the real background," per its README description — gradients sampled under the text's own ink, translucent layers composited) reads 36/47, 11 failing, within the ≤13 ratchet — cited, not re-measured (single-owner rule, PLAN-11 §3.3/§10.2.11). No new colour surface outside `brandtext`'s own accounting was found during P6/P9's page-by-page walk; nothing to raise.

## Exit
All seven method steps recorded above. 0 findings; the AUDIT-10/11 and PLAN-8 accessibility/responsive state holds on this checkout.
